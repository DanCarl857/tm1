# Copy this file to .env and fill in secrets. Do NOT commit the real .env to git.

# API server
PORT=3000
# Path inside container or on host where sqlite DB is stored
DATABASE_PATH=./data/sqlite.db

# JWT secret used for signing tokens (set a strong secret in production)
JWT_SECRET=your_jwt_secret_here

# Seeded admin credentials (used by apps/api/src/seed-admin.ts when DB has no admin)
# Replace with your own values when onboarding local development
SEED_ADMIN_EMAIL=admin@system.com
SEED_ADMIN_PASSWORD=AdminPass123

# Optional: API URL used by frontend during development (frontend dev server)
API_URL=http://localhost:3000

# Optional: CORS origin(s) for the API (comma-separated)
CORS_ORIGINS=http://localhost:4200
